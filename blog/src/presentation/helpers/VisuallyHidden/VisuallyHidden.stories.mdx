import { ArgsTable, Meta } from '@storybook/addon-docs';
import { VisuallyHidden } from './VisuallyHidden';

<Meta title="helpers/VisuallyHidden" component={ VisuallyHidden } />

# VisuallyHidden

Visually hides its children so that they can be read by screen readers.
Use this e. g. when you want to give a link more information that is visible
in elements nearby, but would not be read by a screen reader.
